class Foo {
  var z;
  function init() {
    this.z = 3;
  }

  function foo() {
    return this.z;
  }
}

class Bar extends Foo {
  function init() {
    this(Foo).init();
    this.z = 5;
  }

  function bar() {
    return 5;
  }
}
W
var z = new Bar("ABC");
return z.foo();